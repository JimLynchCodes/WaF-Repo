{
  "name": "waf-socker-server",
  "version": "0.0.1",
  "description": "waf backend socket.io server",
  "main": "app.js",
  "scripts": {
    "start2": "nodemon -e ts --watch src/index.ts --exec \"npm run clean && npm run compile && npm run host:dev-build\"",
    "start": "nodemon -e ts --watch src --exec \"npm run compile && npm start\"",
    "lint": "tslint \"src/**/*.ts\"",
    "test": "jest",
    "test:cov": "jest --coverage",
    "e2e": "echo TODO - e2e tests...",
    "build": "npm run clean && npm run compile",
    "host:dev-build": "node ./dist/index.js",
    "host:prod-build": "node ./dist/index.js",
    "pm2": "TODO - add pm2",
    "deploy": "echo TODO - manual deploy...",
    "compile": "tsc",
    "clean": "rm -rf ./dist"
  },
  "author": "JimTheMan <mrdotjim@gmail.com> (http://www.wisdomofjim.com)",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "@types/jest": "^25.1.3",
    "@types/node": "^13.7.4",
    "jest": "^25.1.0",
    "nodemon": "^2.0.2",
    "tslint": "^6.0.0",
    "typescript": "^3.8.2",
    "typescript-tslint-plugin": "^0.5.5"
  },
  "jest": {
    "moduleFileExtensions": [
      "ts",
      "js",
      "json"
    ],
    "transform": {
      "^.+\\.ts$": "<rootDir>/preprocessor.js"
    },
    "testMatch": [
      "**/*.test.ts"
    ]
  }
}
